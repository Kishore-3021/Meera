use_default_settings: true

general:
  instance_name: "Meera SearXNG"

search:
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "meera-local-searxng-only"
  limiter: false
  image_proxy: false
